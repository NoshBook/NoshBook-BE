// import pool from utils

// declare bare minimum class

// - no fields necessary

// - no constructor necessary

// - declare a static async method to insert a recipeId and the current users id in the cookbook table
//      - validate that the current recipeId doesn't already exist in the cookbook table for this user.
//          - if recipe already exists, throw error
//          - if recipe doesn't exist, perform insert into cookbook table
//      - does this need to return anything for validation purposes?
